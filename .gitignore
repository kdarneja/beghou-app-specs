node_modules
# Anchored to repo root so it only ignores the app build output, NOT the
# committed theme at src/beghou-theme/dist/ (that ships the compiled CSS).
/dist
/dist-ssr
.DS_Store
*.local
.vite

# TypeScript incremental build cache
*.tsbuildinfo

# Local agent / IDE settings
.claude/

# Kendo license — keep out of git
telerik-license.txt
kendo-ui-license.txt
